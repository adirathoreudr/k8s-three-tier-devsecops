# NOTE: Do NOT store real values here. Create this secret via:
#   kubectl create secret generic mongo-secret \
#     --from-literal=MONGO_URI='mongodb://mongodb-service:27017/taskdb' \
#     -n three-tier
#
# The values below are base64 placeholders for documentation only.
apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
  namespace: three-tier
  labels:
    app: mongodb
    tier: database
type: Opaque
data:
  # echo -n 'mongodb://mongodb-service:27017/taskdb' | base64
  MONGO_URI: bW9uZ29kYjovL21vbmdvZGItc2VydmljZToyNzAxNy90YXNrZGI=
