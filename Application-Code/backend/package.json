{
  "name": "three-tier-backend",
  "version": "1.0.0",
  "description": "Three-Tier DevSecOps Backend API",
  "main": "src/index.js",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "mongoose": "^8.0.0",
    "morgan": "^1.10.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node src/index.js",
    "test": "NODE_ENV=test jest --testPathPattern=tests/ --forceExit --verbose",
    "test:coverage": "NODE_ENV=test jest --coverage --forceExit"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 10000
  }
}
